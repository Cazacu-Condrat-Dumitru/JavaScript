//todo use const
/*function ucFirst (str){

  alert(str[0].toUpperCase() + str.slice(1));
  }
  
  ucFirst(prompt('lowercase word'));*/

const phrase=`using const method`;

alert(`${phrase[0].toUpperCase()}${phrase.slice(1)} `);